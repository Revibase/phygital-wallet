/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAuthorityHeaderDecoder,
  getAuthorityHeaderEncoder,
  getMintCapDecoder,
  getMintCapEncoder,
  getProgramPermissionDecoder,
  getProgramPermissionEncoder,
  getSpendCapDecoder,
  getSpendCapEncoder,
  type AuthorityHeader,
  type AuthorityHeaderArgs,
  type MintCap,
  type MintCapArgs,
  type ProgramPermission,
  type ProgramPermissionArgs,
  type SpendCap,
  type SpendCapArgs,
} from "../types/index.js";

export const AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  36, 108, 254, 18, 167, 144, 27, 36,
]);

export function getAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(AUTHORITY_DISCRIMINATOR);
}

export type Authority = {
  discriminator: ReadonlyUint8Array;
  header: AuthorityHeader;
  /**
   * Native SOL spend cap. Meters native lamports AND wallet-owned wrapped SOL
   * (both WSOL native mints) together — one asset to the user. Active when
   * `cap != 0` and `header.policy_version == WALLET_POLICY_VERSION`.
   */
  solCap: SpendCap;
  /** Must be zero; aligns the following mint array for zero-copy reads. */
  policyPadding: number;
  /**
   * Per-asset allowances. Any configured cap blocks decreases of uncapped
   * assets; no caps means no amount limits. WSOL belongs to sol_cap.
   */
  mintCaps: Array<MintCap>;
  /** Explicit per-program overrides of the fixed baseline. */
  programPermissions: Array<ProgramPermission>;
};

export type AuthorityArgs = {
  header: AuthorityHeaderArgs;
  /**
   * Native SOL spend cap. Meters native lamports AND wallet-owned wrapped SOL
   * (both WSOL native mints) together — one asset to the user. Active when
   * `cap != 0` and `header.policy_version == WALLET_POLICY_VERSION`.
   */
  solCap: SpendCapArgs;
  /** Must be zero; aligns the following mint array for zero-copy reads. */
  policyPadding: number;
  /**
   * Per-asset allowances. Any configured cap blocks decreases of uncapped
   * assets; no caps means no amount limits. WSOL belongs to sol_cap.
   */
  mintCaps: Array<MintCapArgs>;
  /** Explicit per-program overrides of the fixed baseline. */
  programPermissions: Array<ProgramPermissionArgs>;
};

/** Gets the encoder for {@link AuthorityArgs} account data. */
export function getAuthorityEncoder(): Encoder<AuthorityArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["header", getAuthorityHeaderEncoder()],
      ["solCap", getSpendCapEncoder()],
      ["policyPadding", getU32Encoder()],
      ["mintCaps", getArrayEncoder(getMintCapEncoder())],
      ["programPermissions", getArrayEncoder(getProgramPermissionEncoder())],
    ]),
    (value) => ({ ...value, discriminator: AUTHORITY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Authority} account data. */
export function getAuthorityDecoder(): Decoder<Authority> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["header", getAuthorityHeaderDecoder()],
    ["solCap", getSpendCapDecoder()],
    ["policyPadding", getU32Decoder()],
    ["mintCaps", getArrayDecoder(getMintCapDecoder())],
    ["programPermissions", getArrayDecoder(getProgramPermissionDecoder())],
  ]);
}

/** Gets the codec for {@link Authority} account data. */
export function getAuthorityCodec(): Codec<AuthorityArgs, Authority> {
  return combineCodec(getAuthorityEncoder(), getAuthorityDecoder());
}

export function decodeAuthority<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Authority, TAddress>;
export function decodeAuthority<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Authority, TAddress>;
export function decodeAuthority<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Authority, TAddress> | MaybeAccount<Authority, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAuthorityDecoder(),
  );
}

export async function fetchAuthority<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Authority, TAddress>> {
  const maybeAccount = await fetchMaybeAuthority(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAuthority<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Authority, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAuthority(maybeAccount);
}

export async function fetchAllAuthority(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Authority>[]> {
  const maybeAccounts = await fetchAllMaybeAuthority(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAuthority(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Authority>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAuthority(maybeAccount));
}
