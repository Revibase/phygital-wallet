/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
} from "@solana/kit";
import {
  getAccountKeyConstraintDecoder,
  getAccountKeyConstraintEncoder,
  getDataConstraintDecoder,
  getDataConstraintEncoder,
  type AccountKeyConstraint,
  type AccountKeyConstraintArgs,
  type DataConstraint,
  type DataConstraintArgs,
} from "./index.js";

export type AccountConstraint = {
  /** Position in this instruction, not in the outer remaining_accounts list. */
  index: number;
  key: AccountKeyConstraint;
  /** Solana owner program, not the token account's encoded owner field. */
  owner: Option<Address>;
  /** Effective privileges passed to CPI, including the wallet PDA signature. */
  isSigner: Option<boolean>;
  isWritable: Option<boolean>;
  /** Account-data predicates require an explicit owner program. */
  data: Array<DataConstraint>;
};

export type AccountConstraintArgs = {
  /** Position in this instruction, not in the outer remaining_accounts list. */
  index: number;
  key: AccountKeyConstraintArgs;
  /** Solana owner program, not the token account's encoded owner field. */
  owner: OptionOrNullable<Address>;
  /** Effective privileges passed to CPI, including the wallet PDA signature. */
  isSigner: OptionOrNullable<boolean>;
  isWritable: OptionOrNullable<boolean>;
  /** Account-data predicates require an explicit owner program. */
  data: Array<DataConstraintArgs>;
};

export function getAccountConstraintEncoder(): Encoder<AccountConstraintArgs> {
  return getStructEncoder([
    ["index", getU8Encoder()],
    ["key", getAccountKeyConstraintEncoder()],
    ["owner", getOptionEncoder(getAddressEncoder())],
    ["isSigner", getOptionEncoder(getBooleanEncoder())],
    ["isWritable", getOptionEncoder(getBooleanEncoder())],
    ["data", getArrayEncoder(getDataConstraintEncoder())],
  ]);
}

export function getAccountConstraintDecoder(): Decoder<AccountConstraint> {
  return getStructDecoder([
    ["index", getU8Decoder()],
    ["key", getAccountKeyConstraintDecoder()],
    ["owner", getOptionDecoder(getAddressDecoder())],
    ["isSigner", getOptionDecoder(getBooleanDecoder())],
    ["isWritable", getOptionDecoder(getBooleanDecoder())],
    ["data", getArrayDecoder(getDataConstraintDecoder())],
  ]);
}

export function getAccountConstraintCodec(): Codec<
  AccountConstraintArgs,
  AccountConstraint
> {
  return combineCodec(
    getAccountConstraintEncoder(),
    getAccountConstraintDecoder(),
  );
}
