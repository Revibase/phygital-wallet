/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountConstraintDecoder,
  getAccountConstraintEncoder,
  getDataConstraintDecoder,
  getDataConstraintEncoder,
  type AccountConstraint,
  type AccountConstraintArgs,
  type DataConstraint,
  type DataConstraintArgs,
} from "./index.js";

export type InstructionRule = {
  /** Nonempty byte prefix identifying the instruction (not necessarily Anchor). */
  selector: ReadonlyUint8Array;
  /** Exact encoded data length when the instruction has a fixed layout. */
  dataLength: Option<number>;
  /** Exact number of instruction account positions, including duplicates. */
  accountCount: Option<number>;
  accounts: Array<AccountConstraint>;
  /** Per-instruction checks; an amount bound here is not a per-tap budget. */
  arguments: Array<DataConstraint>;
};

export type InstructionRuleArgs = {
  /** Nonempty byte prefix identifying the instruction (not necessarily Anchor). */
  selector: ReadonlyUint8Array;
  /** Exact encoded data length when the instruction has a fixed layout. */
  dataLength: OptionOrNullable<number>;
  /** Exact number of instruction account positions, including duplicates. */
  accountCount: OptionOrNullable<number>;
  accounts: Array<AccountConstraintArgs>;
  /** Per-instruction checks; an amount bound here is not a per-tap budget. */
  arguments: Array<DataConstraintArgs>;
};

export function getInstructionRuleEncoder(): Encoder<InstructionRuleArgs> {
  return getStructEncoder([
    ["selector", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ["dataLength", getOptionEncoder(getU16Encoder())],
    ["accountCount", getOptionEncoder(getU8Encoder())],
    ["accounts", getArrayEncoder(getAccountConstraintEncoder())],
    ["arguments", getArrayEncoder(getDataConstraintEncoder())],
  ]);
}

export function getInstructionRuleDecoder(): Decoder<InstructionRule> {
  return getStructDecoder([
    ["selector", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ["dataLength", getOptionDecoder(getU16Decoder())],
    ["accountCount", getOptionDecoder(getU8Decoder())],
    ["accounts", getArrayDecoder(getAccountConstraintDecoder())],
    ["arguments", getArrayDecoder(getDataConstraintDecoder())],
  ]);
}

export function getInstructionRuleCodec(): Codec<
  InstructionRuleArgs,
  InstructionRule
> {
  return combineCodec(getInstructionRuleEncoder(), getInstructionRuleDecoder());
}
