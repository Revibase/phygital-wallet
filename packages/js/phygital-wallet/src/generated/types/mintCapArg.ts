/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

export type MintCapArg = {
  mint: Address;
  /**
   * Allowance in raw asset units; must be nonzero. Saving refills it only when
   * the amount or window changes; an unchanged cap keeps its remaining usage.
   */
  cap: bigint;
  /** 0 => no automatic reset; > 0 => interval in seconds anchored at save time. */
  windowSeconds: bigint;
};

export type MintCapArgArgs = {
  mint: Address;
  /**
   * Allowance in raw asset units; must be nonzero. Saving refills it only when
   * the amount or window changes; an unchanged cap keeps its remaining usage.
   */
  cap: number | bigint;
  /** 0 => no automatic reset; > 0 => interval in seconds anchored at save time. */
  windowSeconds: number | bigint;
};

export function getMintCapArgEncoder(): FixedSizeEncoder<MintCapArgArgs> {
  return getStructEncoder([
    ["mint", getAddressEncoder()],
    ["cap", getU64Encoder()],
    ["windowSeconds", getI64Encoder()],
  ]);
}

export function getMintCapArgDecoder(): FixedSizeDecoder<MintCapArg> {
  return getStructDecoder([
    ["mint", getAddressDecoder()],
    ["cap", getU64Decoder()],
    ["windowSeconds", getI64Decoder()],
  ]);
}

export function getMintCapArgCodec(): FixedSizeCodec<
  MintCapArgArgs,
  MintCapArg
> {
  return combineCodec(getMintCapArgEncoder(), getMintCapArgDecoder());
}
