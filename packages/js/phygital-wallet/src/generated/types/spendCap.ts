/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * A fixed-interval allowance in raw units. `window_seconds == 0` never resets
 * automatically. Positive windows refill on a charge at or after the boundary;
 * `last_reset` anchors the interval. Saving policy preserves usage for a cap left
 * unchanged; changing a cap's amount or window refills and reanchors that cap.
 * Used directly as the SOL cap and embedded (with a mint) in `MintCap`. Derives
 * both Borsh (for the IDL / clients) and `bytemuck::Pod` (for on-chain reads); the
 * padding-free `#[repr(C)]` layout makes those two encodings byte-identical.
 */
export type SpendCap = {
  /** Maximum allowance per period (or lifetime for a zero window). */
  cap: bigint;
  /** Stored allowance; reads do not refill it when an interval passes. */
  remaining: bigint;
  /** Chain timestamp anchoring this interval, not the time of the last payment. */
  lastReset: bigint;
  windowSeconds: bigint;
};

export type SpendCapArgs = {
  /** Maximum allowance per period (or lifetime for a zero window). */
  cap: number | bigint;
  /** Stored allowance; reads do not refill it when an interval passes. */
  remaining: number | bigint;
  /** Chain timestamp anchoring this interval, not the time of the last payment. */
  lastReset: number | bigint;
  windowSeconds: number | bigint;
};

export function getSpendCapEncoder(): FixedSizeEncoder<SpendCapArgs> {
  return getStructEncoder([
    ["cap", getU64Encoder()],
    ["remaining", getU64Encoder()],
    ["lastReset", getI64Encoder()],
    ["windowSeconds", getI64Encoder()],
  ]);
}

export function getSpendCapDecoder(): FixedSizeDecoder<SpendCap> {
  return getStructDecoder([
    ["cap", getU64Decoder()],
    ["remaining", getU64Decoder()],
    ["lastReset", getI64Decoder()],
    ["windowSeconds", getI64Decoder()],
  ]);
}

export function getSpendCapCodec(): FixedSizeCodec<SpendCapArgs, SpendCap> {
  return combineCodec(getSpendCapEncoder(), getSpendCapDecoder());
}
