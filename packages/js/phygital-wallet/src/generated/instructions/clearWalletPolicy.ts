/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PHYGITAL_WALLET_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLEAR_WALLET_POLICY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([226, 224, 82, 15, 201, 69, 123, 12]);

export function getClearWalletPolicyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEAR_WALLET_POLICY_DISCRIMINATOR,
  );
}

export type ClearWalletPolicyInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRentReceiver extends string | AccountMeta<string> = string,
  TAccountPhygitalToken extends string | AccountMeta<string> = string,
  TAccountAuthorityAccount extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRentReceiver extends string
        ? WritableAccount<TAccountRentReceiver>
        : TAccountRentReceiver,
      TAccountPhygitalToken extends string
        ? ReadonlyAccount<TAccountPhygitalToken>
        : TAccountPhygitalToken,
      TAccountAuthorityAccount extends string
        ? WritableAccount<TAccountAuthorityAccount>
        : TAccountAuthorityAccount,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      ...TRemainingAccounts,
    ]
  >;

export type ClearWalletPolicyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClearWalletPolicyInstructionDataArgs = {};

export function getClearWalletPolicyInstructionDataEncoder(): FixedSizeEncoder<ClearWalletPolicyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLEAR_WALLET_POLICY_DISCRIMINATOR }),
  );
}

export function getClearWalletPolicyInstructionDataDecoder(): FixedSizeDecoder<ClearWalletPolicyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClearWalletPolicyInstructionDataCodec(): FixedSizeCodec<
  ClearWalletPolicyInstructionDataArgs,
  ClearWalletPolicyInstructionData
> {
  return combineCodec(
    getClearWalletPolicyInstructionDataEncoder(),
    getClearWalletPolicyInstructionDataDecoder(),
  );
}

export type ClearWalletPolicyInput<
  TAccountAuthority extends string = string,
  TAccountRentReceiver extends string = string,
  TAccountPhygitalToken extends string = string,
  TAccountAuthorityAccount extends string = string,
  TAccountInstructionsSysvar extends string = string,
> = {
  /** The token's authority (ed25519). */
  authority: TransactionSigner<TAccountAuthority>;
  rentReceiver: Address<TAccountRentReceiver>;
  phygitalToken: Address<TAccountPhygitalToken>;
  /** and canonical PDA are validated in the handler (see `SetWalletPolicy`). */
  authorityAccount: Address<TAccountAuthorityAccount>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
};

export function getClearWalletPolicyInstruction<
  TAccountAuthority extends string,
  TAccountRentReceiver extends string,
  TAccountPhygitalToken extends string,
  TAccountAuthorityAccount extends string,
  TAccountInstructionsSysvar extends string,
  TProgramAddress extends Address = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
>(
  input: ClearWalletPolicyInput<
    TAccountAuthority,
    TAccountRentReceiver,
    TAccountPhygitalToken,
    TAccountAuthorityAccount,
    TAccountInstructionsSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): ClearWalletPolicyInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountRentReceiver,
  TAccountPhygitalToken,
  TAccountAuthorityAccount,
  TAccountInstructionsSysvar
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    rentReceiver: { value: input.rentReceiver ?? null, isWritable: true },
    phygitalToken: { value: input.phygitalToken ?? null, isWritable: false },
    authorityAccount: {
      value: input.authorityAccount ?? null,
      isWritable: true,
    },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("rentReceiver", accounts.rentReceiver),
      getAccountMeta("phygitalToken", accounts.phygitalToken),
      getAccountMeta("authorityAccount", accounts.authorityAccount),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
    ],
    data: getClearWalletPolicyInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearWalletPolicyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRentReceiver,
    TAccountPhygitalToken,
    TAccountAuthorityAccount,
    TAccountInstructionsSysvar
  >);
}

export type ParsedClearWalletPolicyInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The token's authority (ed25519). */
    authority: TAccountMetas[0];
    rentReceiver: TAccountMetas[1];
    phygitalToken: TAccountMetas[2];
    /** and canonical PDA are validated in the handler (see `SetWalletPolicy`). */
    authorityAccount: TAccountMetas[3];
    instructionsSysvar: TAccountMetas[4];
  };
  data: ClearWalletPolicyInstructionData;
};

export function parseClearWalletPolicyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClearWalletPolicyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      rentReceiver: getNextAccount(),
      phygitalToken: getNextAccount(),
      authorityAccount: getNextAccount(),
      instructionsSysvar: getNextAccount(),
    },
    data: getClearWalletPolicyInstructionDataDecoder().decode(instruction.data),
  };
}
