/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PHYGITAL_WALLET_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLEAR_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [129, 249, 41, 111, 187, 52, 199, 214],
);

export function getClearAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEAR_AUTHORITY_DISCRIMINATOR,
  );
}

export type ClearAuthorityInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRentReceiver extends string | AccountMeta<string> = string,
  TAccountAuthorityAccount extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRentReceiver extends string
        ? WritableAccount<TAccountRentReceiver>
        : TAccountRentReceiver,
      TAccountAuthorityAccount extends string
        ? WritableAccount<TAccountAuthorityAccount>
        : TAccountAuthorityAccount,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      ...TRemainingAccounts,
    ]
  >;

export type ClearAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClearAuthorityInstructionDataArgs = {};

export function getClearAuthorityInstructionDataEncoder(): FixedSizeEncoder<ClearAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLEAR_AUTHORITY_DISCRIMINATOR }),
  );
}

export function getClearAuthorityInstructionDataDecoder(): FixedSizeDecoder<ClearAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClearAuthorityInstructionDataCodec(): FixedSizeCodec<
  ClearAuthorityInstructionDataArgs,
  ClearAuthorityInstructionData
> {
  return combineCodec(
    getClearAuthorityInstructionDataEncoder(),
    getClearAuthorityInstructionDataDecoder(),
  );
}

export type ClearAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountRentReceiver extends string = string,
  TAccountAuthorityAccount extends string = string,
  TAccountInstructionsSysvar extends string = string,
> = {
  /**
   * Current owner key. Clearing removes the policy and disables the accessory tap
   * (a tap requires a present owner); it does not close the wallet holding funds.
   * The passkey can re-enable the tap by running `set_authority` again.
   */
  authority: TransactionSigner<TAccountAuthority>;
  rentReceiver: Address<TAccountRentReceiver>;
  /**
   * Canonical PDA + authority signer are validated in the handler (Anchor
   * `seeds`/`has_one` can't be expressed in the IDL here — they'd need `?` and
   * a self-reference to `authority_account`). `close` refunds `rent_receiver`.
   */
  authorityAccount: Address<TAccountAuthorityAccount>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
};

export function getClearAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountRentReceiver extends string,
  TAccountAuthorityAccount extends string,
  TAccountInstructionsSysvar extends string,
  TProgramAddress extends Address = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
>(
  input: ClearAuthorityInput<
    TAccountAuthority,
    TAccountRentReceiver,
    TAccountAuthorityAccount,
    TAccountInstructionsSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): ClearAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountRentReceiver,
  TAccountAuthorityAccount,
  TAccountInstructionsSysvar
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    rentReceiver: { value: input.rentReceiver ?? null, isWritable: true },
    authorityAccount: {
      value: input.authorityAccount ?? null,
      isWritable: true,
    },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("rentReceiver", accounts.rentReceiver),
      getAccountMeta("authorityAccount", accounts.authorityAccount),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
    ],
    data: getClearAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRentReceiver,
    TAccountAuthorityAccount,
    TAccountInstructionsSysvar
  >);
}

export type ParsedClearAuthorityInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Current owner key. Clearing removes the policy and disables the accessory tap
     * (a tap requires a present owner); it does not close the wallet holding funds.
     * The passkey can re-enable the tap by running `set_authority` again.
     */
    authority: TAccountMetas[0];
    rentReceiver: TAccountMetas[1];
    /**
     * Canonical PDA + authority signer are validated in the handler (Anchor
     * `seeds`/`has_one` can't be expressed in the IDL here — they'd need `?` and
     * a self-reference to `authority_account`). `close` refunds `rent_receiver`.
     */
    authorityAccount: TAccountMetas[2];
    instructionsSysvar: TAccountMetas[3];
  };
  data: ClearAuthorityInstructionData;
};

export function parseClearAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClearAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      rentReceiver: getNextAccount(),
      authorityAccount: getNextAccount(),
      instructionsSysvar: getNextAccount(),
    },
    data: getClearAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
